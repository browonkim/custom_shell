1. 백그라운드 실행 구현하기 (백그라운드실행: parent 바로실행 wait없음 그냥 실행하면 됨)
단 출력은 해주기 현재 상태등을!

2. exec 이해하고 전체경로 주지 않아도 실행할 방법 고려해보기

3. IO redirectrion > >> < 구현하기
>는 무조건 덮어쓰기
>>는 output redirection 파일이 없으면 새로 생성하고 있다면 뒤에 추가하기
< 는 input을 이걸로 전달하기

4. 파이프 구현하기

5. 파이프 재귀로 구현하기 

6. 최종 저것들 다 섞어서도 구현할 수 있게 (왼쪽부터 오른쪽으로 순서대로 파싱해서 순서대로 처리하기)